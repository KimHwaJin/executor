# Copy to .env for a local Docker run; do not include real secrets in delivery archives.
JUPYTER_ROOT_DIR=/workspace/pv
# Required: supply a non-empty token. Use a Secret in your deployment platform.
JUPYTER_TOKEN=
